- name: "unarchive backend"
  unarchive:
    src: udapeople_backend.tar.gz
    dest: /home/ubuntu/

- name: "executing node"
  shell: |
    cd /home/ubuntu/backend/
    sudo apt install -y npm pm2
    npm install
    pm2 stop default
    pm2 start npm -- start
